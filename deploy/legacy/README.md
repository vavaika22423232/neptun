# Legacy Deploy Configs

This directory keeps older deployment config variants that may still be useful for reference.

Canonical nginx configs live one level up in `deploy/`:

- `nginx-neptun.conf`
- `nginx-neptun-optimized.conf`
- `nginx-http-limits-snippet.conf`
- `nginx-upstream-nextjs.conf`
