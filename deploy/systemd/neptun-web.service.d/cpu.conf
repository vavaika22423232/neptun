[Service]
# Prefer CPU for Next.js cluster vs other user services on the same host.
# Install: sudo mkdir -p /etc/systemd/system/neptun-web.service.d
#          sudo cp deploy/systemd/neptun-web.service.d/cpu.conf /etc/systemd/system/neptun-web.service.d/
#          sudo systemctl daemon-reload && sudo systemctl restart neptun-web
CPUWeight=400
