# Channel Forwarder Bot - Інструкція

## 🎯 Призначення

Автоматично пересилає повідомлення з вихідних каналів до вашого каналу `@mapstransler`.

## 📋 Вихідні канали

1. @UkraineAlarmSignal
2. @kpszsu
3. @war_monitor
4. @napramok
5. @raketa_trevoga
6. @ukrainsiypposhnik

## 🚀 Варіанти запуску

### Варіант 1: Telethon (Рекомендовано)

**Переваги:**
- ✅ Працює з приватними каналами
- ✅ Не потрібно бути адміністратором вихідних каналів
- ✅ Краща надійність
- ✅ Підтримка всіх типів медіа

**Файл:** `channel_forwarder.py`

#### Встановлення залежностей

```bash
pip install telethon
```

#### Перший запуск

```bash
python channel_forwarder.py
```

**Важливо:**
1. При першому запуску потрібно ввести код з SMS
2. Можливо попросить 2FA пароль (якщо налаштовано)
3. Створюється файл сесії `channel_forwarder.session`

#### Наступні запуски

```bash
python channel_forwarder.py
```

Більше не потрібно вводити код!

---

### Варіант 2: Bot API (Обмежений)

**Переваги:**
- ✅ Простіше налаштувати
- ✅ Не потрібен номер телефону

**Недоліки:**
- ❌ Потрібно бути адміністратором ВСІХ каналів
- ❌ Не працює з приватними каналами

**Файл:** `channel_forwarder_bot.py`

#### Встановлення залежностей

```bash
pip install python-telegram-bot
```

#### Налаштування

1. Додайте бота як адміністратора в ВСІ канали (вихідні + цільовий)
2. Отримайте ID каналів через @username_to_id_bot
3. Додайте ID в `SOURCE_CHANNEL_IDS` у файлі

#### Запуск

```bash
python channel_forwarder_bot.py
```

---

## ⚙️ Налаштування

### Змінити цільовий канал

У файлі знайдіть і змініть:
```python
TARGET_CHANNEL = 'mapstransler'  # Ваш канал
```

### Додати/видалити вихідні канали

```python
SOURCE_CHANNELS = [
    'UkraineAlarmSignal',
    'новий_канал',  # Додати
    # 'war_monitor',  # Видалити (закоментувати)
]
```

---

## 🔧 Запуск у фоні

### Linux/macOS - screen

```bash
# Створити сесію
screen -S forwarder

# Запустити бота
python channel_forwarder.py

# Відключитися (бот продовжить працювати)
Ctrl+A, потім D

# Повернутися до сесії
screen -r forwarder
```

### Linux/macOS - nohup

```bash
nohup python channel_forwarder.py > forwarder.log 2>&1 &

# Переглянути логи
tail -f forwarder.log
```

### systemd service (Linux)

Створіть `/etc/systemd/system/channel-forwarder.service`:

```ini
[Unit]
Description=Telegram Channel Forwarder
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/render2
ExecStart=/usr/bin/python3 /path/to/render2/channel_forwarder.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl enable channel-forwarder
sudo systemctl start channel-forwarder
sudo systemctl status channel-forwarder
```

---

## 📊 Моніторинг

### Логи

Всі події пишуться в консоль:
- `📨` - Нове повідомлення отримано
- `✅` - Успішно переслано
- `❌` - Помилка
- `⚠️` - Попередження

### Статистика

При запуску показується:
```
📊 Статистика:
   Вихідних каналів: 6/6
   Цільовий канал: @mapstransler

🎯 Бот запущено! Очікую нові повідомлення...
```

---

## ❓ FAQ

### Бот не пересилає повідомлення

1. **Перевірте доступ до каналів:**
   - Ви підписані на всі вихідні канали?
   - Ви адміністратор цільового каналу?

2. **Перевірте логи:**
   - Чи з'являються повідомлення `📨 Нове повідомлення`?
   - Чи є помилки `❌`?

3. **Перезапустіть бота:**
   ```bash
   # Зупинити (Ctrl+C)
   # Запустити знову
   python channel_forwarder.py
   ```

### "Не вдалося знайти канал"

- Перевірте що ви підписані на канал
- Спробуйте додати `@` перед назвою: `@UkraineAlarmSignal`
- Для приватних каналів використовуйте ID замість username

### "No write access"

- Ви повинні бути адміністратором цільового каналу
- Перевірте що бот має права на публікацію

### Сесія не зберігається

- Файл `channel_forwarder.session` повинен бути в тій же папці
- Не видаляйте цей файл!
- Переконайтеся що є права на запис

---

## 🔐 Безпека

### Захист даних

- ❌ **НЕ публікуйте** файли з токенами/паролями на GitHub
- ❌ **НЕ діліться** файлом `.session`
- ✅ Додайте в `.gitignore`:
  ```
  *.session
  *.session-journal
  channel_forwarder.py
  channel_forwarder_bot.py
  ```

### Рекомендації

1. Використовуйте окремий Telegram акаунт для бота
2. Увімкніть 2FA (двофакторну автентифікацію)
3. Регулярно перевіряйте активні сесії в Telegram

---

## 🛠️ Розширення функціоналу

### Фільтрація повідомлень

Додайте в обробник:
```python
# Пересилати тільки з текстом "тривога"
if message.text and 'тривога' in message.text.lower():
    # пересилати
```

### Затримка між пересиланням

```python
import time

# Перед пересиланням
await asyncio.sleep(2)  # 2 секунди затримки
```

### Зміна формату

```python
# Додати emoji, змінити формат
forward_text = f"🚨 ТРИВОГА!\n"
forward_text += f"Джерело: {source_name}\n"
forward_text += f"{message.text}\n"
forward_text += f"\n#тривога #alerts"
```

---

## 📞 Підтримка

Якщо виникли проблеми:

1. Перевірте логи
2. Перезапустіть бота
3. Перевірте налаштування
4. Створіть issue на GitHub

---

## 📝 Приклади використання

### Базовий запуск

```bash
cd /Users/vladimirmalik/Desktop/render2
python channel_forwarder.py
```

### З логуванням у файл

```bash
python channel_forwarder.py 2>&1 | tee forwarder.log
```

### Автозапуск при завантаженні (macOS)

Створіть `~/Library/LaunchAgents/com.neptun.forwarder.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.neptun.forwarder</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>/Users/vladimirmalik/Desktop/render2/channel_forwarder.py</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
```

Активація:
```bash
launchctl load ~/Library/LaunchAgents/com.neptun.forwarder.plist
```

---

**Успіхів! 🚀**
