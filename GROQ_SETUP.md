# Groq AI Integration Setup

## 🎯 Що це дає?

Groq AI (Llama 3.1 70B) **розумно аналізує текст** повідомлень і **точно визначає** міста для маркерів:

### Приклади:
- ✅ **"БпЛА маневрує в районі Юріївки"** → Правильно розуміє що Юріївка - село, а не район
- ✅ **"БпЛА курсом на Тернівку"** → Нормалізує відмінок (Тернівку → Тернівка)
- ✅ **"Дніпропетровщина:"** → Визначає область (Дніпропетровська область)
- ✅ **"в Павлоградському районі"** → Витягує назву району для точнішого геокодингу

### Переваги:
- 🚀 **Швидкість**: 500+ tokens/sec (миттєва відповідь)
- 💰 **Безкоштовно**: Generous free tier (30 req/min, 14,400/day)
- 🧠 **Розумний**: Розуміє український текст та військовий контекст
- 🎯 **Точність**: Правильно розбирає складні повідомлення

---

## 📝 Налаштування

### 1. Отримати безкоштовний API ключ

1. Відкрийте https://console.groq.com/
2. Зареєструйтеся (Gmail/GitHub)
3. Перейдіть в **API Keys**
4. Натисніть **Create API Key**
5. Скопіюйте ключ (формат: `gsk_...`)

### 2. Додати ключ на Render

1. Відкрийте ваш сервіс на https://dashboard.render.com/
2. Перейдіть в **Environment**
3. Додайте змінну:
   - **Key**: `GROQ_API_KEY`
   - **Value**: `gsk_ваш_ключ_тут`
4. Натисніть **Save Changes**

Render автоматично перезапустить сервіс з новою змінною.

### 3. Локальне тестування (опціонально)

Для локального тестування створіть `.env` файл:

```bash
echo "GROQ_API_KEY=gsk_ваш_ключ_тут" >> .env
```

Або експортуйте змінну:

```bash
export GROQ_API_KEY="gsk_ваш_ключ_тут"
```

---

## ✅ Перевірка роботи

Після налаштування перевірте логи Render:

```
INFO: Groq AI initialized successfully
```

Якщо ключа немає:
```
INFO: Groq AI disabled (no API key)
```

При обробці повідомлень з'являться логи:
```
DEBUG Groq AI: city='Юріївка', district=None, oblast='Дніпропетровська область', confidence=0.95
DEBUG Groq+Photon: 'юріївка' in Павлоградський район, Дніпропетровська область -> (48.746, 36.018)
```

---

## 🔧 Технічні деталі

### Модель
- **llama-3.1-70b-versatile** - найпотужніша безкоштовна модель
- Temperature: 0.1 (детермінований аналіз)
- Max tokens: 300

### Fallback
Якщо Groq недоступний або впевненість < 70%, система використовує:
1. SpaCy NLP (якщо доступний)
2. Regex-based парсинг
3. Photon/Nominatim геокодинг

### Ліміти
- **30 запитів/хвилину**
- **14,400 запитів/день**
- Для вашого навантаження цього **більш ніж достатньо**

---

## 📊 Статистика

Groq обробляє повідомлення лише коли:
- Назва міста неоднозначна (багато міст з такою назвою)
- Потрібен контекст області/району
- Складна граматична форма

Типове навантаження: **5-10 запитів/годину** (далеко в межах ліміту)

---

## 🎉 Готово!

Після налаштування система автоматично використає Groq AI для розумного аналізу повідомлень.

**Маркери будуть ставитися точніше!** 🎯
