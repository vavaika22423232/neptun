__pycache__/
*.pyc
*.log
# Secrets / env (do NOT commit real credentials)
.env
.env.*

# Telegram session artifacts
new_session.txt
*session*
dtek_session.session
*.session

# Caches (keep messages.json tracked intentionally)
opencage_cache.json
hidden_markers.json
# Allow committing visits_recent.json per user request

# Virtual environments
venv/
.venv/

# OS junk
Thumbs.db
.DS_Store

# Mobile apps - Android & iOS
android-app/
app-ios/
ios-app/
NeptunAlarmMapiOS/

templates/admin_old.html.backup
