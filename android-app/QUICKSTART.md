# 🚀 Швидкий старт NEPTUN Android App

## Крок 1: Відкрийте проект в Android Studio

```bash
cd /Users/vladimirmalik/Desktop/render2/android-app
# Відкрийте цю папку в Android Studio
```

## Крок 2: Отримайте Google Maps API Key

1. Перейдіть: https://console.cloud.google.com/
2. Створіть проект або виберіть існуючий
3. APIs & Services → Library → шукайте "Maps SDK for Android" → Enable
4. APIs & Services → Credentials → Create Credentials → API Key
5. Скопіюйте API ключ

## Крок 3: Додайте API ключ

Створіть файл `local.properties`:

```properties
MAPS_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

## Крок 4: Sync і Build

1. В Android Studio: File → Sync Project with Gradle Files
2. Дочекайтеся завершення sync
3. Build → Make Project

## Крок 5: Запустіть

1. Створіть емулятор або підключіть пристрій
2. Натисніть Run ▶️
3. Виберіть пристрій
4. Дочекайтеся встановлення

## 🎉 Готово!

Додаток запуститься і покаже карту України з маркерами загроз!

## 📱 Що ви побачите:

- Карту України
- Маркери подій (ракети, БПЛА, авіація)
- Лічильник активних подій
- Кнопку оновлення
- Індикатор авто-оновлення

## 🔧 Якщо щось не працює:

### Карта не завантажується
```bash
# Перевірте API ключ
cat local.properties

# Перевірте що Maps SDK увімкнено в Google Cloud Console
```

### Build помилки
```bash
# Очистіть і rebuild
./gradlew clean
./gradlew build

# Або в Android Studio:
# Build → Clean Project
# Build → Rebuild Project
```

### Емулятор не запускається
```bash
# Tools → Device Manager → Create Device
# Виберіть Pixel 6 Pro
# Виберіть Android 14 (API 34)
# Finish і запустіть
```

## 📊 API відповідь

Додаток отримує дані з:
```
GET https://neptun.onrender.com/api/events
```

Приклад:
```json
{
  "events": [
    {
      "lat": 50.45,
      "lng": 30.52,
      "text": "Загроза",
      "type": "ракета"
    }
  ]
}
```

## 🎨 Кастомізація

### Змінити інтервал оновлення
`MapViewModel.kt`:
```kotlin
delay(30_000) // 30 секунд → змініть на інше значення
```

### Змінити початкову позицію карти
`MapScreen.kt`:
```kotlin
val ukraineCenter = LatLng(50.45, 30.52) // Київ
```

### Змінити кольори маркерів
`MapScreen.kt`:
```kotlin
fun getMarkerColor(eventType: String): Float {
    // Додайте свої правила
}
```

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте README.md
2. Перегляньте logcat: `adb logcat | grep Neptun`
3. Перевірте що API доступний: `curl https://neptun.onrender.com/api/events`

---

Made with ❤️ for Ukraine 🇺🇦
