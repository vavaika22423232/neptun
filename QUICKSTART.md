# 🎯 ШВИДКИЙ СТАРТ - Channel Forwarder Bot

## Що робить цей бот?

Автоматично **пересилає всі повідомлення** з 6 каналів до вашого каналу `@mapstransler`:

### Вихідні канали (звідки):
1. @UkraineAlarmSignal
2. @kpszsu
3. @war_monitor
4. @napramok
5. @raketa_trevoga
6. @ukrainsiypposhnik

### Цільовий канал (куди):
- @mapstransler

---

## 🚀 Як запустити (3 кроки)

### Крок 1: Встановити залежності

```bash
cd /Users/vladimirmalik/Desktop/render2
python3 -m pip install -r requirements_forwarder.txt
```

### Крок 2: Перший запуск

```bash
./start_forwarder.sh
```

**АБО вручну:**

```bash
python3 channel_forwarder.py
```

### Крок 3: Авторизація (тільки перший раз!)

При першому запуску побачите:

```
Enter phone number: +263781966038
Enter code: 12345  ← Код з SMS
```

Якщо є 2FA (двофакторна автентифікація):
```
Enter 2FA password: ваш_пароль
```

**Готово!** Наступні запуски не потребують коду.

---

## ✅ Що побачите після запуску

```
🚀 Запуск Channel Forwarder Bot...
✅ Підключено до Telegram
🔍 Перевірка доступу до каналів...
✅ Цільовий канал знайдено: Maps Transler
✅ Вихідний канал знайдено: Ukraine Alarm Signal (@UkraineAlarmSignal)
✅ Вихідний канал знайдено: КПЗСУ (@kpszsu)
...

📊 Статистика:
   Вихідних каналів: 6/6
   Цільовий канал: @mapstransler

🎯 Бот запущено! Очікую нові повідомлення...

📨 Нове повідомлення з @UkraineAlarmSignal
✅ Переслано до @mapstransler (всього: 1)
```

---

## 📋 Формат пересланих повідомлень

Кожне повідомлення буде виглядати так:

```
📢 Джерело: @UkraineAlarmSignal
⏰ Час: 14:35:22 05.12.2024
────────────────────────────────────────

🚨 Повітряна тривога в Київській області!
```

---

## 🛑 Як зупинити бота

Натисніть:
```
Ctrl + C
```

Побачите:
```
⏹️ Бот зупинено користувачем
```

---

## 🔄 Запуск у фоновому режимі

### macOS/Linux - screen (рекомендовано)

```bash
# Створити сесію
screen -S forwarder

# Запустити бота
python3 channel_forwarder.py

# Відключитися (Ctrl+A, потім D)
# Бот продовжує працювати!

# Повернутися пізніше
screen -r forwarder

# Зупинити
Ctrl+C
```

### macOS/Linux - nohup

```bash
# Запустити у фоні
nohup python3 channel_forwarder.py > forwarder.log 2>&1 &

# Дивитися логи в реальному часі
tail -f forwarder.log

# Зупинити
ps aux | grep channel_forwarder
kill [PID]
```

---

## 🔧 Налаштування

### Змінити цільовий канал

Відкрийте `channel_forwarder.py` і змініть:

```python
TARGET_CHANNEL = 'mapstransler'  # Ваш канал
```

### Додати нові вихідні канали

```python
SOURCE_CHANNELS = [
    'UkraineAlarmSignal',
    'kpszsu',
    'war_monitor',
    'напрямок',  # Додати новий
    # 'raketa_trevoga',  # Видалити (закоментувати)
]
```

### Змінити формат повідомлень

Знайдіть в коді:

```python
forward_text = f"📢 Джерело: @{source_chat.username or source_name}\n"
forward_text += f"⏰ Час: {datetime.now().strftime('%H:%M:%S %d.%m.%Y')}\n"
```

Змініть як вам потрібно!

---

## ❓ Вирішення проблем

### "Не вдалося знайти канал"

**Причина:** Ви не підписані на канал

**Рішення:**
1. Відкрийте Telegram
2. Підпишіться на всі 6 каналів
3. Перезапустіть бота

### "No write access"

**Причина:** Ви не адміністратор цільового каналу

**Рішення:**
1. Переконайтеся що ви адміністратор @mapstransler
2. Дайте собі права на публікацію повідомлень

### "Session file not found"

**Причина:** Видалено файл сесії

**Рішення:**
1. Запустіть бота знову
2. Введіть код з SMS ще раз
3. НЕ видаляйте файл `channel_forwarder.session`!

### Повідомлення не пересилаються

**Перевірте:**
1. Бот запущений? (дивіться термінал)
2. Є нові повідомлення в вихідних каналах?
3. Дивіться логи - чи є помилки?

### Дублікати повідомлень

**Причина:** Запущено декілька копій бота

**Рішення:**
```bash
# Знайти всі процеси
ps aux | grep channel_forwarder

# Вбити зайві
kill [PID]
```

---

## 📁 Структура файлів

```
render2/
├── channel_forwarder.py              ← Основний бот (Telethon)
├── channel_forwarder_bot.py          ← Альтернатива (Bot API)
├── requirements_forwarder.txt        ← Залежності
├── start_forwarder.sh                ← Швидкий запуск
├── CHANNEL_FORWARDER_README.md       ← Повна документація
├── QUICKSTART.md                     ← Цей файл
└── channel_forwarder.session         ← Створюється після авторизації
```

---

## 🔐 Безпека

### ⚠️ НЕ РОБІТЬ:
- ❌ НЕ публікуйте `.session` файли
- ❌ НЕ діліться API ключами
- ❌ НЕ коммітьте в Git конфіги з токенами

### ✅ ЗРОБІТЬ:
- ✅ Додайте в `.gitignore`:
  ```
  *.session
  *.session-journal
  channel_forwarder.py
  ```
- ✅ Використовуйте окремий Telegram акаунт
- ✅ Увімкніть 2FA

---

## 📊 Моніторинг

### Статистика в реальному часі

Бот показує:
- `📨` - Отримано нове повідомлення
- `✅` - Успішно переслано (+ лічильник)
- `❌` - Помилка
- `⚠️` - Попередження

### Приклад логів:

```
📨 Нове повідомлення з @UkraineAlarmSignal
✅ Переслано до @mapstransler (всього: 1)
📨 Нове повідомлення з @kpszsu
✅ Переслано до @mapstransler (всього: 2)
📨 Нове повідомлення з @war_monitor
✅ Переслано до @mapstransler (всього: 3)
```

---

## 🎓 Корисні команди

### Переглянути що працює

```bash
ps aux | grep channel_forwarder
```

### Дивитися логи (якщо запущено через nohup)

```bash
tail -f forwarder.log
```

### Перезапустити

```bash
# Зупинити
Ctrl+C

# Запустити знову
python3 channel_forwarder.py
```

---

## 💡 Поради

1. **Тестування:** Спочатку створіть тестовий канал і протестуйте там
2. **Резервна копія:** Збережіть файл `.session` в безпечне місце
3. **Логи:** Зберігайте логи для аналізу помилок
4. **Автозапуск:** Налаштуйте systemd/launchd для автостарту

---

## 📞 Потрібна допомога?

1. Прочитайте повну документацію: `CHANNEL_FORWARDER_README.md`
2. Перевірте логи бота
3. Перезапустіть бота
4. Перевірте підключення до інтернету

---

## ✨ Готово!

Тепер ви можете:
- ✅ Отримувати всі повідомлення з 6 каналів в одному місці
- ✅ Не пропускати важливу інформацію
- ✅ Мати повний архів повідомлень
- ✅ Легко інтегрувати з іншими системами

**Успіхів! 🚀**

---

*Версія 1.0 - 5 грудня 2024*
