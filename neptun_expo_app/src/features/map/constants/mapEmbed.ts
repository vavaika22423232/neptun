/** Production map embed — must match Flutter `MapTab._buildMapUrl`. */
export const MAP_EMBED_URL = 'https://neptun.in.ua/?embed=1&theme=dark';

export const MAP_WEBVIEW_LOAD_TIMEOUT_MS = 20_000;
export const MAP_WEBVIEW_MIN_HEIGHT = 80;
export const MAP_WEBVIEW_IOS_MOUNT_DELAY_MS = 350;
