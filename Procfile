# Procfile для Render - запуск обох сервісів

# Основний Flask додаток
web: python app.py

# Channel Forwarder Bot (опціонально, якщо потрібен окремий worker)
# worker: python channel_forwarder_render.py
