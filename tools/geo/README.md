# Geo Tools

One-off OSM/Overpass utilities for building or inspecting Ukraine settlement data.

These scripts are not part of the worker runtime or CI. Prefer the maintained worker gazetteer code
under `nextjs-app/worker/geo/` for production data changes.
